<?xml version="1.0"?>
<!DOCTYPE varlistentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                              "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % entities.ent SYSTEM "entities.ent">
%entities.ent;
]>
<varlistentry>
<term><option>--string-vacancies=<replaceable>n</replaceable></option></term>
<listitem><para>Set the minimum
<indexterm>
<primary>--string-vacancies=n</primary>
</indexterm>
number of characters that should be available for the user's control
sequences and font names, after the compiler's own error messages are
stored.  Must be at least 25000 less than
<varname>pool_size</varname>, but doesn't need to be nearly that
large.</para></listitem>
</varlistentry>
